# Local dev shows the full site; production defaults to the holding page
# (lib/holding.ts). Run `NEXT_PUBLIC_SITE_MODE=holding pnpm dev` to preview
# the holding page locally.
NEXT_PUBLIC_SITE_MODE=full
